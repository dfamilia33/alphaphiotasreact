import React from "react"

export default function Homepage() { 
    return (
    <div style={{ color: `red` }}>¡Hola Mundo! <br></br>
    Routing for a spanish translated version would go through here.<br></br>
    We would be able to reuse components so only the text would need to be <br></br>
    changed. Only a single attribute such as lang='es' would need to <br></br>
    be passed into the component.
    </div>
    )
}
